{
  "name": "query-athena",
  "version": "2.0.0",
  "private": true,
  "description": "Lambda function generated by Amplify",
  "main": "index.js",
  "type": "module",
  "dependencies": {},
  "scripts": {
    "clean": "rm -f *.js athena/*.js || true",
    "build:before": "npm run clean & npm install --production=false",
    "build": "esbuild --outdir=. *.ts athena/*.ts",
    "build:after": "npm install --production=true"
  }
}
